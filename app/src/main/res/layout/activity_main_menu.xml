<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/parentPadding"
    android:gravity="end">


    <Button
        android:id="@+id/btn_wheel"
        android:layout_width="@dimen/gearBackgroundSize"
        android:layout_height="@dimen/gearBackgroundSize"
        android:background="@color/transparentGearBackground"
        android:drawableTop="@drawable/wheel"
        android:onClick="openSettings" />

    <TextView
        android:id="@+id/welcomeText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/welcome"
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        android:textColor="@color/purple_200"
        android:textSize="@dimen/welcomeTextSize" />

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="@dimen/welcomeImageHeight"
        app:srcCompat="@drawable/flags" />

    <Button
        android:id="@+id/start"
        android:text="@string/start_quiz"
        android:onClick="startQuiz"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/exit_button"
        android:text="@string/exit"
        android:onClick="showAlertDialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>



</LinearLayout>
